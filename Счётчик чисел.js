for(i = 0; i < 20; i++) {

    if(i % 3 === 0) continue

    console.log(i % 3, `Число: ${i}`)
}

// Цель:

// Выводит все числа от 1 до 20, кроме чисел, кратных 3 (например, 3, 6, 9 и т.д.).
// Попробуй использовать continue для пропуска ненужных чисел.
